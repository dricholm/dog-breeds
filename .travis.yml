language: node_js
node_js:
  - 'node'

install:
  - npm i -g codecov

after_success:
  - codecov

branches:
  only:
    - master

script:
  - npm run test:ci
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
