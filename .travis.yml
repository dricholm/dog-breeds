language: node_js
node_js:
  - 'node'

branches:
  only:
    - master

script:
  - npm run test:ci
  - npm run build

after_success:
  - bash <(curl -s https://codecov.io/bash)

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
